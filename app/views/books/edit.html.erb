<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <div class="user-edit-image">Image</div>
    <%= f.attachment_field :profile_image %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <p><%= f.submit 'Update Book' %></p>
<% end %>